import React from "react";
import ReactDOM from "react-dom/client";
import "@google/model-viewer";
import QuadroCafe from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <QuadroCafe />
  </React.StrictMode>
);
